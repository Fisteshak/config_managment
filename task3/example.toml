# name
title = "TOML Example"

# here is tables
# this comment is multiline
# asd

a = 10
b = 15.0
c = 2.0
x = "?(a b * c +)"
y = "?(b a mod)"
z = "?(a b max)"

[database]
enabled = 1
temp_targets = { cpu = 79.5, case = 72.0 }

[servers]
[servers.alpha]
ip = "10.0.0.1"
role = "frontend"

[servers.beta]
ip = "10.0.0.2"
role = "backend"